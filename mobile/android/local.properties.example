# Machine-local — copy to local.properties (gitignored) or let scripts/build-android-apk.ps1 generate it.
# Paths from I_Tuoi_Versetti toolchain (config/android-build.json is the canonical source in repo):
sdk.dir=D\:\\Android\\Sdk
