# History

Eventi significativi del repository (release, campagne, milestone architetturali).

Per cronologia operativa dettagliata: `KB/journal.md` (locale, gitignored).

Per decisioni architetturali: [`../architecture/adr/`](../architecture/adr/).
